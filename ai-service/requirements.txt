# Full development + training dependencies.
# The production Docker image uses requirements-prod.txt.
#
# Keep the core inference packages pinned to THE SAME versions as
# requirements-prod.txt so local training produces artifacts that load
# cleanly on the server without version mismatch errors.

# ── Inference (pinned to match production) ────────────────────────
numpy==1.24.3
pandas==2.2.2
scikit-learn==1.3.0
xgboost==3.2.0
joblib==1.3.2
flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
python-dotenv==1.0.1

# ── Training only ─────────────────────────────────────────────────
shap==0.45.1
scipy==1.13.1
imbalanced-learn==0.12.3

# ── Testing only ──────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0
