# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/

# Training dataset — not needed for inference, regenerated by train.py
data/career_dataset.csv

# Model artefacts are committed to git so the Docker image can include them
# without a training step at build time.  Current total: ~5 MB.
# To update: run models/train.py locally, then commit the new .joblib files.

# Environment
.env

# Logs
*.log
